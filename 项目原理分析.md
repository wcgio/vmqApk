# V免签 Android 监控端 项目原理分析 (v5.0 旗舰增强版)

## 项目概述
V免签是一个**个人收款监控系统**，通过 Android 手机监听支付通知并回调服务端。v5.0 重点强化了在大规模并发下的日志追踪与系统稳定性。

---

## 核心技术原理

### 一、精准监听 (Android 16 穿透)
针对 Android 16 (API 36) 的通知隔离：
- **深度 Bundle 扫描**：遍历 `Notification.extras` 中所有字段，捕获“实时活动”下的金额信息。
- **动态去重**：5秒内相同金额/标题的通知自动合并，防止重复回调。

### 二、智能日志持久化 (v5.0 New)
引入 `LogHelper` 异步日志归档系统：
- **Daily Rolling 策略**：
    - 每日自动创建 `yyyy-MM-dd.log` 文件。
    - 自动保留最近 7 天日志，超出周期自动物理删除旧文件。
- **异步 I/O 设计**：
    - 核心业务日志（支付成功、API 错误）通过单线程 Executor 异步写入磁盘。
    - **性能优势**：即使每日 5000+ 笔订单产生大量流水，也不会阻塞主进程的通知监听逻辑。
- **日志过滤**：仅持久化关键业务事件，静默心跳等高频无用信息不计入磁盘，延长 Flash 寿命。

### 三、保活与抗拦截 🔋
- **前台服务**：声明 `dataSync` 类型并补全 API 34+ 必须的权限。
- **唤醒锁**：持有 `PARTIAL_WAKE_LOCK` 确保锁屏状态下 CPU 不停工。
- **状态同步**：Pill 状指示灯通过 Broadcast 实时反馈服务器连通性。

---

## 工作流程图 (v5.0)

```
1.  App 启动， PackageManager 动态注入版本号
    ↓
2.  检测通知权限 (引导解除 Android 16 受限设置)
    ↓
3.  监控服务通过 dataSync 类型强力启动
    ↓
4.  初始化 LogHelper 磁盘归档系统
    ↓
5.  建立长效心跳，同步 Status Pill 状态
    ↓
6.  监听到支付通知 → 深度解析金额 → 5秒去重
    ↓
7.  生成签名 → 执行异步 HTTP 回调
    ↓
8.  LogHelper 异步记录支付流水到 [当日日期].log
    ↓
9.  后台定期检查并清理 7 天前的历史日志
```

---

## UI 与交互
- **全环境自适应**：支持系统级的深色模式切换。
- **Glassmorphism**：配置弹窗采用 80% 不透明度的高斯模糊效果。
- **沉浸式体验**：状态栏与应用底色完美对齐。

---
*本项目旨在研究 Android 系统的服务保活与通知机制，请遵守相关法律法规。*